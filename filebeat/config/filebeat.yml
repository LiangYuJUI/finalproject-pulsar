filebeat.inputs:
- type: log
  enabled: true
  paths:
    - /usr/share/filebeat/logs/log-*.json
  fields:
    log_type: "app_logs"
  fields_under_root: true

# output設定在容器中印出來 之後要串pulsar
output.console:
  pretty: true
